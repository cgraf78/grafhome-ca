# Managed by grafhome-ca. Rendered copies must not contain secrets.
Host *
    UserKnownHostsFile ~/.ssh/known_hosts
    GlobalKnownHostsFile ${GRAFHOME_CA_SYSTEM_SSH_DIR}/ssh_known_hosts ${GRAFHOME_CA_SSH_TRUST_DIR}/ssh_known_hosts
    RevokedHostKeys ${GRAFHOME_CA_SSH_TRUST_DIR}/revoked_host_keys
